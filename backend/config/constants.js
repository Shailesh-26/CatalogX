// ── Smart Library — Global Constants ─────────────────────────
// Change values here and they apply everywhere automatically

module.exports = {
  BORROW_DAYS:   3,    // How many days a student can keep a book
  FINE_PER_DAY:  5,    // Fine in ₹ per day after due date
  MAX_BORROWS:   3,    // Max books a student can borrow at once
};